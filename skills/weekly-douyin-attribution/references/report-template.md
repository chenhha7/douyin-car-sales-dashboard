# 周度经营归因报告模板

# 抖音本地生活卖车周度经营归因

**分析周期**：`<当前周开始>` 至 `<当前周结束>`  
**对比周期**：`<基准周开始>` 至 `<基准周结束>`  
**数据更新时间**：`<时间>`  
**任务批次**：`<run_id>`

## 一、执行摘要

- 本周核心结果：`<一句话说明锁单、创开、退款等最重要变化>`
- 主要影响环节：`<拆解树中贡献最大的因子及影响量>`
- 主要影响范围：`<服务商/来源/直播间/区域/门店>`
- 建议优先动作：`<动作与责任角色>`
- 证据等级：`<fact / supported_inference / hypothesis / causal_estimate>`

## 二、数据质量

| 检查项 | 状态 | 结果 | 处理建议 |
| --- | --- | --- | --- |
| 周期完整性 | `<pass/block>` | `<结果>` | `<建议>` |
| 数据更新时间 | `<pass/block>` | `<结果>` | `<建议>` |
| 主键重复 | `<pass/warn/block>` | `<结果>` | `<建议>` |
| 事件顺序 | `<pass/warn/block>` | `<结果>` | `<建议>` |
| 来源/门店映射 | `<pass/warn/block>` | `<结果>` | `<建议>` |
| 成熟用户组 | `<pass/block>` | `<结果>` | `<建议>` |

若出现 `block`，报告到此为止，只保留数据问题，不生成经营结论。

## 三、核心指标变化

| 指标 | 当前周 | 基准周 | 变化 | 样本量 | 是否异常 |
| --- | ---: | ---: | ---: | ---: | --- |
| `<指标>` | `<值>` | `<值>` | `<绝对/百分点变化>` | `<n>` | `<是/否>` |

## 四、拆解树定位

### `<核心指标>`

```text
<指标公式>
```

| 因子 | 当前周 | 基准周 | 因子变化 | 对目标指标贡献量 | 贡献占比 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `<因子>` | `<值>` | `<值>` | `<变化>` | `<影响量>` | `<占比>` |

**解读**：`<用业务语言说明主要是规模变化还是转化效率变化，不推断未经验证的原因。>`

## 五、多维定位

### 主要负向影响

| 维度 | 维值 | 当前周 | 基准周 | 当前分母 | 影响量 | 证据等级 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `<区域>` | `<维值>` | `<值>` | `<值>` | `<n>` | `<影响量>` | `fact` |

### 主要正向影响

| 维度 | 维值 | 当前周 | 基准周 | 当前分母 | 影响量 |
| --- | --- | ---: | ---: | ---: | ---: |
| `<维度>` | `<维值>` | `<值>` | `<值>` | `<n>` | `<影响量>` |

## 六、明细下钻

- 明细类型：`<已创建未开启/退款订单/成熟未锁单等>`
- 筛选条件：`<结构化条件>`
- 命中数量：`<n>`
- 文件位置：`<仅内部可访问的脱敏明细路径>`
- 待业务核实：`<原因假设>`

## 七、任务草稿

| 优先级 | 问题 | 数据证据 | 建议动作 | 责任角色 | 截止时间 | 验证指标 |
| --- | --- | --- | --- | --- | --- | --- |
| P1 | `<问题>` | `<事实>` | `<动作>` | `<角色>` | `<日期>` | `<指标>` |

## 八、渠道价值

| 用户类型 | 用户占比 | 30 日锁单率 | 成交周期中位数 | 解读 |
| --- | ---: | ---: | ---: | --- |
| 新增贡献 | `<值>` | `<值>` | `<值>` | `<解释>` |
| 历史未成交激活 | `<值>` | `<值>` | `<值>` | `<解释>` |
| 存量促转 | `<值>` | `<值>` | `<值>` | `<解释>` |

## 九、策略效果

- 策略：`<SOP/投流/商品等>`
- 上线日期与范围：`<日期、区域或门店>`
- 评估设计：`<双重差分/等长周期前后观察>`
- 目标指标：`<变化及置信区间，如可得>`
- 护栏指标：`<退款率等>`
- 结论：`<明确因果强度>`

## 十、下周验证

1. `<待验证假设>`
2. `<待补数据>`
3. `<下一次复盘指标和日期>`
